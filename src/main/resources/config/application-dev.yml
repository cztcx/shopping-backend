# ------------------------Datasource------------------------------
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shop?characterEncoding=utf8&serverTimezone=UTC&serverTimezone=GMT%2B8
    username: root
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB
# ------------------------logging------------------------------
logging:
  config: classpath:config/logback-dev.xml

